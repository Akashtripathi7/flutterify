# QUESTION BANKS — Logic + Interview (Questions Only)

> No answers, no solutions. Use alongside the two roadmaps. Difficulty tags: **(B)** Beginner, **(I)** Intermediate, **(A)** Advanced, **(E)** Expert, **(Sr)** Senior, **(St)** Staff.
> Treat every question as a dry-run-first exercise: predict, then verify.

---

# BANK 1 — 150 LOOP QUESTIONS

## Beginner (1–45)
1. Print numbers 1 to 100. (B)
2. Print numbers 100 down to 1. (B)
3. Print all even numbers 1–50. (B)
4. Print all odd numbers 1–50. (B)
5. Print multiples of 5 up to 100. (B)
6. Print multiples of 3 between 30 and 90. (B)
7. Sum of numbers 1 to N. (B)
8. Sum of even numbers 1 to 100. (B)
9. Sum of odd numbers 1 to 100. (B)
10. Count numbers divisible by 4 between 1 and 200. (B)
11. Print squares of 1 to 20. (B)
12. Print cubes of 1 to 15. (B)
13. Multiplication table of 7 up to ×10. (B)
14. Multiplication table of N up to ×20. (B)
15. Print all tables 1 to 10. (B)
16. Count down from N to 1 then print "Lift off". (B)
17. Print every 3rd number from 1 to 60. (B)
18. Print numbers 1–20, but the word "even"/"odd" beside each. (B)
19. Print the alphabet A–Z. (B)
20. Print the alphabet Z–A. (B)
21. Sum of all multiples of 3 or 5 below 100. (B)
22. Count how many multiples of 6 are below 500. (B)
23. Print N asterisks on one line. (B)
24. Print N lines each containing one asterisk. (B)
25. Print numbers 1–N separated by commas, no trailing comma. (B)
26. Average of numbers 1 to N. (B)
27. Print first 10 multiples of a given number. (B)
28. Print numbers from A to B (inclusive, A ≤ B). (B)
29. Print numbers from A to B descending. (B)
30. Sum of squares of 1 to N. (B)
31. Count vowels printed when looping over a fixed string. (B)
32. Print each character of a string on its own line. (B)
33. Print a string reversed, character by character. (B)
34. Count characters in a string using a loop (no `.length`). (B)
35. Print "FizzBuzz" basics: numbers 1–30, each on its own line. (B)
36. Print only the numbers 1–50 that end in 0 or 5. (B)
37. Print powers of 2 up to 1024. (B)
38. Print powers of 3 up to 6561. (B)
39. Print the first 12 triangular numbers (1, 3, 6, 10, …). (B)
40. Sum digits of a two-digit number (loop or arithmetic). (B)
41. Print N then N-1 … then 1, all on one line. (B)
42. Print a countdown timer string from 10 to 0. (B)
43. Print numbers 1–N, replacing multiples of 3 with "x". (B)
44. Print how many times you must add 7 to reach or exceed 100. (B)
45. Loop a list of 5 fixed numbers and print each doubled. (B)

## Intermediate (46–95)
46. Classic FizzBuzz 1–100 (Fizz/Buzz/FizzBuzz). (I)
47. Reverse the digits of an integer (arithmetic only). (I)
48. Check if a number is a palindrome. (I)
49. Count the digits of an integer. (I)
50. Sum the digits of an integer. (I)
51. Find the largest digit in a number. (I)
52. Find the smallest digit in a number. (I)
53. Check if a number is prime. (I)
54. Print all primes from 2 to 100. (I)
55. Count primes below N. (I)
56. Print all factors of N. (I)
57. Count factors of N. (I)
58. Prime factorization of N. (I)
59. Check if N is a perfect number (equals sum of proper divisors). (I)
60. Print the first N Fibonacci numbers. (I)
61. Print Fibonacci numbers below a limit L. (I)
62. Check if a number is in the Fibonacci sequence. (I)
63. Compute factorial of N with a loop. (I)
64. Compute N choose K using loops. (I)
65. GCD of two numbers (Euclidean). (I)
66. LCM of two numbers. (I)
67. Print the multiplication table grid 1–10 (nested). (I)
68. Print all pairs (i, j) with i + j == 10, i ≤ j. (I)
69. Count digits that are even in a number. (I)
70. Sum only the odd digits of a number. (I)
71. Reverse only the order of digits but keep sign for negatives. (I)
72. Convert a decimal number to binary using loops. (I)
73. Convert binary (as int digits) to decimal. (I)
74. Find the number of set bits (1s) in a number's binary form. (I)
75. Print the Collatz sequence from N until it reaches 1. (I)
76. Count steps in the Collatz sequence for N. (I)
77. Print the first N rows of Floyd's triangle. (I)
78. Check if a number is an Armstrong number. (I)
79. Print all Armstrong numbers below 1000. (I)
80. Sum of a geometric series with loop. (I)
81. Approximate e using the series sum of 1/n!. (I)
82. Print the multiplication table only for odd multipliers. (I)
83. Find all pairs of primes that sum to an even number N (Goldbach intro). (I)
84. Count how many numbers 1–N are coprime with N. (I)
85. Print Pascal's triangle, N rows. (I)
86. Print a star pyramid of N rows. (I)
87. Print a hollow square of side N. (I)
88. Print a right triangle of increasing numbers. (I)
89. Print a diamond of stars, size N. (I)
90. Print a number spiral plan for 4×4 (describe loop bounds). (I)
91. Find the sum of the diagonal of an N×N number grid. (I)
92. Print a calendar-style week grid (7 columns) for 1–N. (I)
93. Count how many perfect squares are ≤ N. (I)
94. Print all numbers ≤ N whose digit sum is divisible by 3. (I)
95. Print the longest run of consecutive increasing integers in a fixed list. (I)

## Advanced (96–130)
96. Print all prime numbers up to N using the Sieve of Eratosthenes (describe the marking loop). (A)
97. Count twin primes below N. (A)
98. Find the Nth prime number. (A)
99. Print all palindromic numbers below N. (A)
100. Find the largest palindrome that is a product of two 2-digit numbers. (A)
101. Print the multiplication of two numbers using only addition (loop). (A)
102. Compute power(base, exp) using fast exponentiation (loop, halving). (A)
103. Print a number pyramid that mirrors increasing then decreasing per row. (A)
104. Print a hollow diamond, size N. (A)
105. Print a butterfly pattern, size N. (A)
106. Print a hollow butterfly pattern. (A)
107. Print Pascal's triangle using only the previous row (no factorial). (A)
108. Print a concentric "ring number" square pattern. (A)
109. Print numbers in a clockwise spiral for an N×N grid. (A)
110. Print numbers in a counter-clockwise spiral. (A)
111. Generate a ZigZag traversal of an N×M grid. (A)
112. Find the maximum sum of any contiguous K consecutive numbers in a list (window). (A)
113. Find the minimum sum window of size K. (A)
114. Count the number of windows of size K whose sum exceeds T. (A)
115. Find the longest run of equal consecutive elements. (A)
116. Run-length encode a fixed string ("aaabb" → a3b2). (A)
117. Print the digits of N! (factorial) without overflow, using a digit-array loop. (A)
118. Multiply two large numbers represented as digit lists (schoolbook loop). (A)
119. Add two large numbers represented as digit lists. (A)
120. Find the first missing positive integer in a fixed list using index-as-hash loop. (A)
121. Detect a cycle in a sequence defined by repeated function application (loop with two speeds). (A)
122. Compute the digital root of N (repeat digit-sum until single digit). (A)
123. Find the smallest number whose factorial exceeds a given value. (A)
124. Print all combinations of a 3-digit lock (000–999) — count, don't store. (A)
125. Count how many integers ≤ N have no repeated digits. (A)
126. Find the longest increasing contiguous subarray (length and bounds). (A)
127. Compute a prefix-sum array, then answer 3 range-sum queries. (A)
128. Find a contiguous subarray summing to a target (positive numbers). (A)
129. Count subarrays whose sum equals K (prefix sum + frequency map). (A)
130. Find the equilibrium index of a list. (A)

## Expert / Interview-level (131–150)
131. Maximum subarray sum (Kadane's idea — derive the single-pass loop). (E)
132. Maximum product subarray (track min and max). (E)
133. Trapping rain water: total water trapped over a height list (two-pointer loop). (E)
134. Container with most water (two pointers). (E)
135. Longest substring without repeating characters (variable window). (E)
136. Minimum window covering all required characters (shrinking window). (E)
137. Longest substring with at most K distinct characters. (E)
138. Find the majority element appearing > N/2 times (Boyer–Moore loop). (E)
139. Move all zeros to the end in place, preserving order (two pointer). (E)
140. Dutch national flag: sort 0s, 1s, 2s in one pass. (E)
141. Merge K sorted lists conceptually with loops (describe the merge loop). (E)
142. Find the next greater element for each item using a stack-driven loop. (E)
143. Compute stock span / next warmer day using a monotonic loop. (E)
144. Sliding window maximum for each window of size K (deque loop). (E)
145. Count inversions in a list during a merge-style loop. (E)
146. Implement binary search and explain the loop invariant. (E)
147. Find the peak element in a list with a binary-search-style loop. (E)
148. Rotate a list by K positions in place using reversal loops. (E)
149. Detect if a list has a duplicate within distance K (window + set). (E)
150. Given click-stream events, find the busiest fixed-length time window (real-world window). (E)

---

# BANK 2 — 150 DATA MANIPULATION QUESTIONS

## Beginner: Lists, Sets, Maps basics (1–45)
1. Print all elements of a list with index. (B)
2. Find whether a value exists in a list; report first index. (B)
3. Count elements greater than a threshold. (B)
4. Count occurrences of a specific value. (B)
5. Sum all numbers in a list. (B)
6. Average of a list of numbers. (B)
7. Find the maximum element. (B)
8. Find the minimum element. (B)
9. Find the index of the maximum. (B)
10. Find both max and min in a single pass. (B)
11. Reverse a list (new list). (B)
12. Reverse a list in place. (B)
13. Concatenate two lists. (B)
14. Check if a list is empty and handle it gracefully. (B)
15. Get the first and last element safely. (B)
16. Print only even numbers from a list. (B)
17. Build a new list of each element doubled. (B)
18. Build a list of squares of each element. (B)
19. From names, build a list of their lengths. (B)
20. Count how many strings start with a given letter. (B)
21. Join a list of strings with a separator. (B)
22. Find the longest string in a list. (B)
23. Find the shortest string in a list. (B)
24. Create a Set from a list and report its size. (B)
25. Check if two lists share any common element. (B)
26. Add an element to a Map and read it back. (B)
27. Iterate a Map's keys and values. (B)
28. Check if a key exists in a Map. (B)
29. Provide a default when a Map key is missing. (B)
30. Count entries in a Map matching a condition on values. (B)
31. Sum all values in a Map. (B)
32. Find the key with the largest value. (B)
33. Build a Map from two parallel lists (keys, values). (B)
34. Remove an entry from a Map by key. (B)
35. Merge two simple Maps (later wins on conflict). (B)
36. Convert a list of pairs into a Map. (B)
37. Count how many times each first letter appears across a word list. (B)
38. Find duplicates count in a list (how many appear more than once). (B)
39. Filter a list to values within a numeric range. (B)
40. Partition a list into evens and odds (two lists). (B)
41. Find the running total list (cumulative sums). (B)
42. Replace all negatives in a list with 0 (new list). (B)
43. Clamp all values in a list to [min, max]. (B)
44. Count nulls in a nullable list and skip them in a sum. (B)
45. Build a frequency Map of characters in a string. (B)

## Intermediate: filter/map/sort/group/dedup (46–95)
46. Filter even numbers, then square them. (I)
47. Map prices to discounted prices (10% off). (I)
48. Sort numbers ascending; then descending. (I)
49. Sort strings alphabetically; then by length. (I)
50. Sort a list of objects by a numeric field. (I)
51. Sort by score desc, then name asc (tiebreaker). (I)
52. Group numbers into even/odd buckets (Map of lists). (I)
53. Group words by first letter (Map<String, List>). (I)
54. Group objects by a category field. (I)
55. Deduplicate a list preserving first-seen order. (I)
56. Intersection of two lists. (I)
57. Union of two lists (no duplicates). (I)
58. Difference of two lists (A minus B). (I)
59. Symmetric difference of two lists. (I)
60. Find the first non-repeating element. (I)
61. Find the most frequent element. (I)
62. Find the top-3 most frequent elements. (I)
63. Find all elements appearing exactly twice. (I)
64. Invert a Map (value → key); handle duplicate values. (I)
65. Merge two frequency Maps by summing counts. (I)
66. Build word-frequency Map from a sentence and return sorted by count. (I)
67. Flatten a list of lists one level deep. (I)
68. Chunk a list into groups of size K. (I)
69. Zip two lists into a list of pairs. (I)
70. Unzip a list of pairs into two lists. (I)
71. Find pairs of numbers that sum to a target. (I)
72. Count pairs with a given difference. (I)
73. Check if two strings are anagrams. (I)
74. Group a list of words into anagram clusters. (I)
75. Remove all occurrences of a value from a list. (I)
76. Replace every occurrence of X with Y in a list. (I)
77. Rotate a list left by K. (I)
78. Find the second-largest distinct value. (I)
79. Find the median of a list. (I)
80. Find the mode of a list. (I)
81. Compute min, max, mean, and range in one structure. (I)
82. Normalize a list of numbers to 0..1. (I)
83. Bucket numbers into histogram bins of width W. (I)
84. Find the longest common prefix across a list of strings. (I)
85. Count words in a paragraph and ignore punctuation. (I)
86. Sort a Map by its values descending. (I)
87. Filter a Map to entries whose value passes a test. (I)
88. Transform a Map's values (e.g., multiply each by 2). (I)
89. Build a Map of category → total from a list of records. (I)
90. Find duplicates across two lists and where they overlap. (I)
91. Find elements present in all of three lists. (I)
92. Merge sorted list A and sorted list B into one sorted list. (I)
93. Deduplicate a sorted list in place. (I)
94. Find the K smallest elements. (I)
95. Find the K largest elements. (I)

## Advanced: nested data, analytics, reports, pagination, ranking (96–130)
96. Sum every element of a 2D matrix. (A)
97. Sum each row and each column of a matrix. (A)
98. Find the max in each row. (A)
99. Transpose a matrix. (A)
100. Rotate a matrix 90° clockwise. (A)
101. Search for a value in a row-and-column-sorted matrix. (A)
102. Flatten an arbitrarily nested list (recursion/stack plan). (A)
103. Given `List<Map>` of users with roles, list users having a given role. (A)
104. Build role → list-of-users Map from user records. (A)
105. Find the average age of users grouped by city. (A)
106. From sales records, compute total revenue (qty × price). (A)
107. Revenue per region (grouped sum). (A)
108. Top-3 products by revenue. (A)
109. Average order value across orders. (A)
110. Build a report Map: total revenue, best region, best product, order count. (A)
111. Compute month-over-month change from a list of monthly totals. (A)
112. Compute a 7-day rolling average from daily metrics. (A)
113. Detect anomaly days exceeding 2× the rolling average. (A)
114. Rank items by score; assign dense ranks with ties. (A)
115. Rank items with standard competition ranking (1,2,2,4). (A)
116. Given a leaderboard, return a user's rank even if off the visible page. (A)
117. Paginate a list: given page size and page number, return the slice. (A)
118. Handle the last partial page and out-of-range page numbers. (A)
119. Cursor-based pagination: given a cursor id, return the next N items. (A)
120. Build a nested category tree from flat parent-id records. (A)
121. Compute totals roll-up over a category tree. (A)
122. Merge two `List<Map>` datasets on a key (join). (A)
123. Left-join two datasets, filling missing with defaults. (A)
124. Pivot records into a matrix (rows = region, cols = month, cells = revenue). (A)
125. Deduplicate records by composite key, keeping the latest by timestamp. (A)
126. Find the busiest hour from a list of event timestamps. (A)
127. Sessionize events: group events into sessions split by 30-min gaps. (A)
128. Compute distinct active users per day from event logs. (A)
129. Build a frequency distribution and find the percentile values (p50, p90). (A)
130. Detect the top-K trending items by growth between two periods. (A)

## Expert: real systems (131–150)
131. Wallet: process credits/debits to a running balance; reject overdrafts. (E)
132. Wallet: compute minimum balance over a statement period. (E)
133. Banking: detect ≥3 debits within any 10-minute window (sliding window over timestamps). (E)
134. Banking: produce a categorized statement with subtotals and a grand total. (E)
135. E-commerce: compute cart total with quantities and per-item price. (E)
136. E-commerce: apply tiered discounts and decide cumulative vs. best-of. (E)
137. E-commerce: apply a coupon (flat/percent) capped at a max discount. (E)
138. Inventory: decrement stock per order; flag items hitting reorder threshold. (E)
139. Inventory: split a cart into in-stock vs. backordered using a stock Map. (E)
140. POS: compute change with fewest notes from a denomination list (note greedy failure cases). (E)
141. Rewards: accrue points (1 per ₹100), redeem capped at % of bill. (E)
142. Rewards: compute loyalty tier from rolling 12-month spend. (E)
143. Orders: build an order state pipeline (placed → packed → shipped → delivered) and validate transitions. (E)
144. Orders: detect duplicate orders by idempotency key within a window. (E)
145. Offline sync: order a queue of pending operations, dedup, and resolve conflicts (last-write-wins). (E)
146. Analytics: build a spend-insights summary (monthly total, category breakdown, MoM change, top merchant, >20% alert). (E)
147. Fraud: flag accounts whose spend deviates > 2 std-dev from their own mean. (E)
148. Recommendation: co-occurrence counts of items bought together; top related items per product. (E)
149. Search: rank results by a weighted score (relevance, recency, popularity) and paginate. (E)
150. Reconciliation: match two transaction lists (internal vs. bank), report unmatched on each side. (E)

---

# BANK 3 — 50 DEBUGGING QUESTIONS

> For each: identify the bug, explain *why* it happens, and state the fix approach. Do not assume the comment is correct.

## Logic / loops / data (1–25)
1. A sum loop starts `sum = 0` but iterates `i < n` instead of `i <= n` — off by one. Find the symptom.
2. A `while` loop never updates its condition variable. Identify the infinite loop.
3. A swap uses two assignments without a temp and loses a value. Diagnose.
4. A factorial returns 0 because it initializes the product to 0, not 1.
5. A max-finder initializes `max = 0` and fails on all-negative input.
6. A nested loop reuses the same index variable name, breaking the inner loop.
7. A prime check returns true for 1 and for all even numbers. Find both bugs.
8. A reverse-digits routine drops the last digit due to wrong order of `% 10` and `~/ 10`.
9. A palindrome check mutates the original number and reports wrong results on reuse.
10. A frequency map uses `m[k] = m[k] + 1` and crashes on the first occurrence (null). Diagnose.
11. A list filter modifies the list while iterating it, causing a concurrent-modification error.
12. An average divides integers and truncates the decimal unexpectedly.
13. A pagination slice throws a range error on the last partial page.
14. A "remove duplicates" uses a Set but loses the original order, breaking a test.
15. A sort comparator returns a bool instead of an int and behaves erratically.
16. Sorting by two keys applies them in the wrong order, so the tiebreaker dominates.
17. A two-pointer palindrome check has pointers that cross incorrectly and misreports odd-length cases.
18. A sliding-window sum recomputes the whole window each step (correct result, wrong complexity) — identify the inefficiency.
19. A prefix-sum range query is off by one at the boundaries.
20. A matrix transpose swaps elements twice, returning the original matrix.
21. A "top-K" routine sorts ascending and returns the K smallest by mistake.
22. A grouping routine overwrites the bucket list instead of appending.
23. A running-balance loop applies debits as credits due to a sign error.
24. A discount tier uses `>` where `>=` is needed and misses boundary amounts.
25. A money calculation uses doubles and produces ₹0.30000000004 — diagnose the rounding issue.

## Dart / async (26–38)
26. A `late final` field is read before assignment and throws at runtime. Diagnose.
27. A nullable field is force-unwrapped with `!` and crashes only sometimes. Explain the race.
28. A `Future` is created but never awaited, so an error is swallowed silently.
29. Code expects `Future.microtask` to run after an event-queue `Future` but sees it run first. Explain ordering.
30. A custom class used as a Map key never matches because `==`/`hashCode` aren't overridden.
31. A `Stream` listener is added but never cancelled, leaking memory after the screen closes.
32. An `async` function's exception isn't caught by an outer `try/catch` because the future isn't awaited.
33. A heavy JSON parse on the UI isolate janks the app; identify why `compute` is needed.
34. A closure captures a loop variable and all callbacks see the final value. Diagnose (var vs. capture).
35. `int` overflow assumptions from another language bite a large multiplication on web (JS number) targets.
36. A `const` map is mutated at runtime and throws "Unsupported operation". Explain.
37. A generic method does `is T` and behaves unexpectedly due to type handling. Diagnose.
38. An `async*` generator never emits because nothing listens to the returned stream.

## Flutter (39–50)
39. `setState() called after dispose()` from a delayed callback. Diagnose the lifecycle race.
40. A `ListView` inside a `Column` throws "unbounded height". Explain via constraints.
41. A `Row` of text overflows with a yellow-black stripe. Diagnose the constraint and the fix family.
42. A stateful list item keeps the wrong state after reordering because of missing keys.
43. A `Provider` `Consumer` rebuilds the whole screen on every change; identify the over-rebuild.
44. "Could not find an ancestor Provider/InheritedWidget" — diagnose where `of(context)` was called.
45. An `AnimationController` is created without `vsync` / never disposed, leaking a ticker.
46. Images cause memory to climb without bound; identify the missing cache sizing / disposal.
47. A `FutureBuilder` refetches on every rebuild because the future is created inside `build`.
48. A `GlobalKey` is reused in two places, causing "multiple widgets used the same GlobalKey".
49. A heavy `build` method does network/JSON work inline, causing jank; identify the misplaced work.
50. A bottom sheet's controller is read after the widget is unmounted; diagnose the `mounted` guard gap.

---

# BANK 4 — 50 OUTPUT PREDICTION QUESTIONS

> Predict exact output (or "compile error" / "runtime error") before running.

## Logic & loops (1–18)
1. `print(2 + 3 * 4 - 1);`
2. `print(10 ~/ 3); print(10 % 3); print(10 / 3);`
3. `var s = 0; for (var i = 1; i <= 4; i++) s += i; print(s);`
4. `var p = 1; for (var i = 1; i <= 5; i++) p *= i; print(p);`
5. `for (var i = 0; i < 3; i++) for (var j = 0; j < 2; j++) stdout.write('$i$j ');`
6. `var i = 0; while (i < 5) { if (i == 3) break; i++; } print(i);`
7. `for (var i = 1; i <= 5; i++) { if (i % 2 == 0) continue; stdout.write(i); }`
8. `var n = 16; var c = 0; while (n > 1) { n ~/= 2; c++; } print(c);`
9. `print(5 > 3 && 2 > 4);`
10. `print(true || (1 ~/ 0 == 0));`  (short-circuit?)
11. `var x = 7; print(x++ + ++x);`
12. `var a = 'ab'; a = a * 3; print(a);`  (valid?)
13. `print('5' + 5.toString());`
14. `print([1, 2, 3].length + {1, 1, 2}.length);`
15. `var m = {'a': 1}; m['a'] = m['a']! + 4; print(m);`
16. `print([1, 2, 3].reversed.toList());`
17. `print([3, 1, 2]..sort());`
18. `print('hello'.split('').reversed.join());`

## Dart semantics (19–35)
19. `const a = [1, 2]; const b = [1, 2]; print(identical(a, b));`
20. `var a = [1, 2]; var b = [1, 2]; print(identical(a, b)); print(a == b);`
21. `print(0.1 + 0.2 == 0.3);`
22. `int? x; print(x ?? 99);`
23. `var list = <int>[]; print(list.first);`  (what error?)
24. `print((5).runtimeType); print((5.0).runtimeType);`
25. `var n = null; print(n is int); print(n == null);`
26.
```dart
void main() {
  print('A');
  Future(() => print('B'));
  Future.microtask(() => print('C'));
  print('D');
}
```
27.
```dart
void main() async {
  print(1);
  await Future.delayed(Duration.zero);
  print(2);
  print(3);
}
```
28.
```dart
void main() {
  Future(() => print(1));
  Future.microtask(() => print(2));
  Future(() => print(3)).then((_) => print(4));
  print(5);
}
```
29.
```dart
String f() { try { return 'a'; } finally { print('cleanup'); } }
void main() => print(f());
```
30.
```dart
void main() {
  var l = [1, 2, 3];
  var doubled = l.map((e) => e * 2);
  l.add(4);
  print(doubled.toList());
}
```
31.
```dart
class A { String get name => 'A'; }
class B extends A { String get name => 'B->${super.name}'; }
void main() => print(B().name);
```
32.
```dart
mixin M { String tag() => 'M'; }
class C with M { String tag() => 'C->${super.tag()}'; }
void main() => print(C().tag());
```
33.
```dart
void main() {
  var fns = <Function>[];
  for (var i = 0; i < 3; i++) { fns.add(() => i); }
  print(fns.map((f) => f()).toList());
}
```
34.
```dart
void main() {
  enum Status { ok, fail }
  // is this valid where placed? predict.
}
```
35.
```dart
void main() {
  final r = (1, name: 'x');
  print(r.$1);
  print(r.name);
}
```

## Flutter-flavored (36–50)
36. A `const Text('hi')` widget appears in two places — how many widget *instances* exist conceptually, and will identical const widgets rebuild?
37. `setState(() {})` with an empty body — does the UI rebuild? Predict.
38. Calling `setState` twice synchronously in one handler — how many rebuilds occur in the frame?
39. A `StatelessWidget`'s `build` runs; its parent rebuilds 3 times — how many times does this child's `build` run (assume not `const`)?
40. A `const` child of a rebuilding parent — how many times does the child rebuild?
41. `MediaQuery.of(context).size` is read in `build`; the keyboard opens — does `build` re-run? Predict why.
42. A `FutureBuilder` whose future is created in `build`, with parent rebuilding — how often does the future re-run?
43. An `InheritedWidget` updates; a descendant that called `dependOnInheritedWidgetOfExactType` — does it rebuild?
44. A descendant that read the inherited data via `getElementForInheritedWidgetOfExactType` (no dependency) — does it rebuild?
45. `ListView(children: [...100 widgets])` vs `ListView.builder(itemCount: 100)` — which creates all 100 elements up front?
46. A `Column` with two `Expanded(flex: 1)` and one `Expanded(flex: 2)` — predict the height split.
47. `Container(width: 500)` inside a parent giving tight 100px width — predict final width.
48. A `Timer.periodic` started in `initState` but not cancelled in `dispose` — predict the long-term symptom.
49. An `AnimationController` with `lowerBound: 0, upperBound: 1` driven by a `Tween<double>(begin: 100, end: 200)` — predict the value range emitted.
50. Two sibling widgets both call `Provider.of<T>(context, listen: true)`; `T` notifies once — how many of them rebuild?

---

# BANK 5 — 25 REAL BUSINESS PROBLEMS

> For each: define inputs/outputs, list edge cases, and design the data-processing logic. Treat as design-and-implement assignments (no solutions here).

1. **Wallet ledger** — process a transaction stream into a running balance with overdraft protection and a per-day summary.
2. **Fraud detection** — flag accounts with ≥3 debits in any 10-minute window and spend >2σ above their own mean.
3. **Bank statement generator** — group transactions by category and month with subtotals, grand total, and opening/closing balance.
4. **POS checkout** — compute bill total, tax, tiered discount, coupon, and minimal-change breakdown; print a receipt model.
5. **Inventory management** — process orders, decrement stock, flag reorders, and split carts into in-stock/backordered.
6. **Rewards engine** — accrue points, enforce redemption caps, compute loyalty tier from rolling 12-month spend.
7. **E-commerce cart pricing** — quantities, per-item discounts, cart-level coupon, max-discount cap, and final tax.
8. **Order lifecycle** — validate state transitions (placed→packed→shipped→delivered→returned) and reject illegal jumps.
9. **Idempotent payments** — dedupe duplicate payment requests by idempotency key within a time window.
10. **Spend insights dashboard** — monthly totals, category breakdown, MoM change, top merchant, and a >20% spike alert.
11. **Subscription billing** — prorate mid-cycle upgrades/downgrades and compute the next invoice.
12. **Leaderboard service** — ranks with ties, pagination, and "your rank" even when off-page.
13. **Search ranking** — weighted score (relevance + recency + popularity), then paginate results.
14. **Recommendation (co-purchase)** — build item co-occurrence counts and return top related items per product.
15. **Sessionization** — group raw events into sessions split by 30-minute inactivity gaps; report session count and duration.
16. **DAU/MAU analytics** — distinct active users per day and rolling 30-day actives from event logs.
17. **A/B test readout** — bucket users, aggregate conversions per variant, and compute lift (descriptive only).
18. **Delivery ETA aggregation** — combine live position pings into a smoothed ETA with anomaly rejection.
19. **Reconciliation** — match internal vs. bank transactions on amount+time window; report unmatched both sides.
20. **Tax slab calculator** — apply progressive income-tax slabs (New Tax Regime style) with rebates and cess.
21. **EMI scheduler** — generate an amortization schedule (principal/interest split per month) for a loan.
22. **Pricing experiments** — apply dynamic surge multipliers within bounds and log effective price per request.
23. **Offline sync queue** — order pending writes, dedupe, resolve last-write-wins conflicts, and mark sync status.
24. **Notification batching** — collapse a burst of events into a single digest per user per time window.
25. **Refund processing** — validate refundable amount against the order, partial refunds, and ledger adjustment.

---

# INTERVIEW QUESTION BANKS (Dart, Flutter, Internals, Performance, Architecture, State Mgmt, Android, iOS, System Design, Debugging)

> Organized Beginner → Intermediate → Advanced → Senior → Staff. Answer aloud as in a panel.

## BANK 6 — DART INTERVIEW QUESTIONS
**Beginner**
1. `var` vs `final` vs `const`? (B)
2. What is null safety and why does it matter? (B)
3. List vs Set vs Map — when each? (B)
4. What is `?.`, `??`, `!`? (B)
5. Difference between `==` and `identical`? (B)

**Intermediate**
6. Named vs positional vs optional parameters? (I)
7. What is a factory constructor and when is it needed? (I)
8. `extends` vs `implements` vs `with`? (I)
9. What are mixins and the `on` keyword? (I)
10. What are generics and bounded type parameters? (I)
11. Getters/setters vs fields — when? (I)
12. What is library privacy (`_name`)? (I)

**Advanced**
13. Explain Dart's event loop, microtask and event queues. (A)
14. Future vs Stream; single-subscription vs broadcast. (A)
15. How is `async`/`await` compiled (state machine)? (A)
16. What are isolates and why no shared memory? (A)
17. JIT vs AOT and where each is used. (A)
18. What is tree shaking and what defeats it? (A)
19. Records, patterns, sealed classes — uses? (A)

**Senior**
20. Explain generational GC and how leaks happen in a GC'd language. (Sr)
21. Covariance/contravariance in Dart's type system. (Sr)
22. `Zone` and `runZonedGuarded` for error handling. (Sr)
23. Reified generics — implications for `is T`. (Sr)
24. Design a type-safe `Result<S, E>` with sealed classes. (Sr)

**Staff**
25. Critique "Dart is just Java with null safety." (St)
26. Design an app-wide async error strategy (log, never crash UI). (St)
27. When would you choose isolates over async, with concrete numbers? (St)
28. How would you enforce immutability conventions across a large codebase? (St)

## BANK 7 — FLUTTER (GENERAL) INTERVIEW QUESTIONS
**Beginner**
1. Stateless vs Stateful widget? (B)
2. What is `BuildContext`? (B)
3. What does `setState` do? (B)
4. What is a `key` at a basic level? (B)
5. Hot reload vs hot restart? (B)

**Intermediate**
6. State lifecycle methods and what goes in each? (I)
7. When to use which key type? (I)
8. `Expanded` vs `Flexible` vs `Spacer`? (I)
9. `ListView` vs `ListView.builder`? (I)
10. What is `InheritedWidget` used for? (I)

**Advanced**
11. The three trees and why they exist. (A)
12. Element reconciliation and `canUpdate`. (A)
13. Constraints → sizes → position layout rule. (A)
14. Implicit vs explicit animations. (A)
15. What is a Sliver and a `CustomScrollView`? (A)

**Senior**
16. Walk runApp → first frame end to end. (Sr)
17. When is a `GlobalKey` justified and its cost? (Sr)
18. How `InheritedWidget` underpins Provider/Theme/MediaQuery. (Sr)
19. RepaintBoundary: when it helps vs hurts. (Sr)

**Staff**
20. Teach a mental model to debug any rendering bug (5 questions). (St)
21. Design the rendering strategy for a media-heavy infinite feed. (St)
22. When would you drop to a custom RenderObject in production, and how do you de-risk it? (St)

## BANK 8 — FLUTTER INTERNALS INTERVIEW QUESTIONS
**Intermediate**
1. What happens after `main()`? (I)
2. What does `WidgetsFlutterBinding.ensureInitialized` do? (I)
3. Widget vs Element vs RenderObject responsibilities. (I)

**Advanced**
4. BuildOwner vs PipelineOwner. (A)
5. Build → Layout → Paint → Composite → Rasterize phases. (A)
6. What is the Layer tree and compositing? (A)
7. SchedulerBinding and vsync. (A)
8. Element lifecycle: mount/update/deactivate/unmount. (A)

**Senior**
9. Exactly what gets marked dirty on a leaf `setState`. (Sr)
10. Why recreating widgets every frame is cheap. (Sr)
11. How `BuildContext` maps to the Element tree; why `of` is fast. (Sr)
12. Constraint propagation for nested unbounded widgets. (Sr)

**Staff**
13. Narrate one frame from vsync to pixels, naming all bindings. (St)
14. Where would you hook to measure/optimize the build phase framework-side? (St)
15. Explain the engine/framework/embedder split and what you'd change to port to a new platform. (St)

## BANK 9 — FLUTTER PERFORMANCE INTERVIEW QUESTIONS
**Intermediate**
1. What is jank and the 16ms budget? (I)
2. Why use `const` constructors? (I)
3. `ListView.builder` and why lazy building matters. (I)

**Advanced**
4. UI-thread vs raster-thread jank — how to tell apart. (A)
5. What is shader compilation jank and mitigation? (A)
6. RepaintBoundary and layer caching. (A)
7. Reducing rebuild scope: const, widget extraction, selectors. (A)

**Senior**
8. DevTools timeline triage for a janky frame. (Sr)
9. Diagnose a memory leak with the memory view. (Sr)
10. Image memory blow-ups and `cacheWidth`. (Sr)

**Staff**
11. Build and enforce a performance budget in CI. (St)
12. Cut p95 frame time 40% on a legacy screen — method and proof. (St)
13. Startup-time optimization from process launch to first interactive frame. (St)

## BANK 10 — FLUTTER ARCHITECTURE INTERVIEW QUESTIONS
**Intermediate**
1. MVC vs MVVM in Flutter terms. (I)
2. What is a repository and why? (I)
3. Feature-first vs layer-first folders. (I)

**Advanced**
4. Clean Architecture layers and the dependency rule. (A)
5. Use cases/interactors — when over-engineering? (A)
6. DI approaches: constructor, get_it, Riverpod, injectable. (A)

**Senior**
7. Define repository vs data source vs use case crisply. (Sr)
8. Modularization: enforcing boundaries, avoiding cyclic deps. (Sr)
9. Dependency scoping (singleton/per-feature/per-request). (Sr)

**Staff**
10. Design module boundaries + DI for a 30-feature super-app. (St)
11. Write a state-management + architecture convention for 40 engineers. (St)
12. Plan an incremental migration off a legacy architecture without a rewrite. (St)

## BANK 11 — STATE MANAGEMENT INTERVIEW QUESTIONS
**Intermediate**
1. When is `setState` enough? (I)
2. Provider: watch vs read vs select. (I)
3. Ephemeral vs app vs server-cache state. (I)

**Advanced**
4. Riverpod vs Provider: what context-independence buys. (A)
5. BLoC vs Cubit trade-offs; buildWhen/listenWhen. (A)
6. Model a login flow as BLoC states (sealed). (A)
7. GetX/Redux trade-offs at scale. (A)

**Senior**
8. Rebuild granularity ranked across libraries. (Sr)
9. Testability ranked across libraries. (Sr)
10. Fix an over-rebuilding Consumer with Selector. (Sr)

**Staff**
11. Choose state mgmt for a 5-person vs 50-person team — defend both. (St)
12. Design conventions to prevent state-management chaos org-wide. (St)

## BANK 12 — NATIVE ANDROID INTERVIEW QUESTIONS
**Intermediate**
1. Activity lifecycle and where Flutter sits. (I)
2. APK vs AAB. (I)
3. What is the AndroidManifest for? (I)

**Advanced**
4. Services, foreground services, WorkManager — when each. (A)
5. Deep links vs App Links and verification. (A)
6. Gradle build types vs flavors. (A)

**Senior**
7. Android background execution limits and effect on Flutter sync. (Sr)
8. R8/ProGuard shrinking breaking reflection plugins. (Sr)
9. Full Android release pipeline for a Flutter app. (Sr)

**Staff**
10. Design background sync respecting Doze/limits across OEMs. (St)
11. Diagnose a release-only "missing class" crash to keep-rules. (St)

## BANK 13 — NATIVE iOS INTERVIEW QUESTIONS
**Intermediate**
1. iOS app lifecycle states. (I)
2. AppDelegate vs SceneDelegate. (I)
3. What matters in Info.plist for Flutter? (I)

**Advanced**
4. Universal Links vs URL schemes. (A)
5. APNs push flow and entitlements. (A)
6. CocoaPods and Xcode build for Flutter plugins. (A)

**Senior**
7. iOS background execution constraints and sync design. (Sr)
8. Diagnose an iOS-only pod/build failure. (Sr)

**Staff**
9. Wrap a native iOS SDK (no plugin) with a clean Dart API. (St)
10. Design secure on-device storage using Keychain across both platforms. (St)

## BANK 14 — SYSTEM DESIGN INTERVIEW QUESTIONS
**Intermediate**
1. Design a simple notes app with offline support. (I)
2. Design a paginated feed. (I)

**Advanced**
3. Design Instagram's feed (caching, prefetch, slivers). (A)
4. Design WhatsApp messaging (offline queue, receipts). (A)
5. Design Uber's live-tracking UI (location stream, map perf). (A)

**Senior**
6. Design Swiggy/Zomato live order tracking under peak load. (Sr)
7. Design a wallet with ledger integrity and idempotent payments. (Sr)
8. Design an offline-first POS with hardware integrations. (Sr)

**Staff**
9. Design a banking app: security, SSL pinning, audit, graceful offline. (St)
10. Design an AI assistant app: LLM chat with streaming responses, tool/function calling, token-cost budgets, caching, safety guardrails, and offline draft queue. (St)
11. Design the sync engine: idempotency, ordering, conflict resolution, partial failures. (St)

## BANK 15 — DEBUGGING INTERVIEW QUESTIONS
**Intermediate**
1. App rebuilds entire screen on each keystroke — where do you look? (I)
2. "RenderFlex overflowed" — your diagnostic steps. (I)
3. FutureBuilder refetches constantly — likely cause. (I)

**Advanced**
4. setState-after-dispose async crash — trace and guard. (A)
5. Memory climbs on a gallery screen — leak-hunting plan. (A)
6. "Could not find ancestor Provider" — root cause. (A)

**Senior**
7. Scroll janks only on low-end Android — hypotheses and tools. (Sr)
8. Token-refresh interceptor infinite loop — diagnosis and guard. (Sr)
9. Offline writes lost after reconnect — trace the sync path. (Sr)

**Staff**
10. p95 latency spike for 2% of users on one OS version — playbook to root cause and rollback. (St)
11. Intermittent data corruption in offline conflict resolution — design a reproducible diagnosis. (St)
12. Establish a team-wide, repeatable triage runbook for production Flutter incidents. (St)

---

*End of question banks. Counts: Loops 150 · Data Manipulation 150 · Debugging 50 · Output Prediction 50 · Business Problems 25 · plus 10 interview banks across five difficulty tiers.*
