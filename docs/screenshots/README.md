# Screenshots

Drop your PNG screenshots here using these exact filenames — the main README
embeds them automatically:

| Filename          | What to capture                                            |
|-------------------|------------------------------------------------------------|
| `login.png`       | The login / sign-in screen                                 |
| `dashboard.png`   | The main dashboard ("My learnings" — day cards + heatmap)  |
| `day.png`         | A day page showing the three tracks + question list        |
| `question.png`    | A question with a generated in-depth answer + flashcards   |
| `flashcards.png`  | The flashcards / quick-revision accordion                  |

Recommended: capture on a wide window (≈1440px) so the layout looks its best.
